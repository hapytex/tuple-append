module Data.Tuple.AppendSpec where

import Data.Tuple.Append(TupleAddL((<++)), TupleAddR((++>)), TupleAppend((+++)))

import Test.Hspec

spec :: Spec
spec = do
  describe "besselDenominators" $ fail "foo"
